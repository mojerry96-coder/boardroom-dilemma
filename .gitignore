# Dependencies and build output
node_modules/
dist/
coverage/
*.tsbuildinfo
*.log
.DS_Store

# Local tooling
.claude/

# Source material that is not published: course script, layout mockups, package archives
*.zip
*.pdf
boardroom_dilemma_replication_package/references/

# Raw generation files (the app ships the optimised copies in app/public)
media/originals/
media/video/
media/build/
media/preview/
media/audio/
media/talking/

# Old stand-in stills derived from the mockups (no longer used by the app)
app/public/standins/

# Logo animation source and retired stand-ins (the app ships the converted copies)
media/*.mov
media/unused_standins/

# Layout mockups from the replacement bundle
boardroom_dilemma_exact_replacement_bundle/
